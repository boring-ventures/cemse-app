import CVManager from '@/app/components/cv/CVManager';
import React from 'react';

/**
 * CV Manager Route
 * Main entry point for CV management functionality
 * Renders the complete CV Manager component with all features
 */

export default function CVScreen() {
  return <CVManager />;
}